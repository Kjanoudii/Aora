import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const page = () => {
  return (
    <View className='flex-1 items-center justify-center  '>
      <Text className='flex justify-center'>page</Text>
    </View>
  )
}

export default page

const styles = StyleSheet.create({})